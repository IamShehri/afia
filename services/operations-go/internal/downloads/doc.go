// Package downloads provides resumable, bounded downloads: HTTP client policy,
// resume/ETag, disk preflight, streaming digest, and temporary files. Uses a dedicated
// bounded pool.
// TODO(S08): Implement resumable bounded downloads and digest streaming.
package downloads
