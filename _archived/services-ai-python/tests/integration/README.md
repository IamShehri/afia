# ai-python integration tests

Run the exact pinned OpenMed version against non-sensitive known-answer text. Packaging
tests run on a clean macOS profile without system Python.

<!-- TODO(S02): Known-answer OpenMed NER/PHI integration runs. -->
