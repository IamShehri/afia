# operations-go migrations

Operations-database migrations. Independent from workspace migrations. Operations
database loss may require model/runtime repair but must never make a project unreadable.

<!-- TODO: Add V001 covering schema_migrations, jobs, job_dependencies, job_events, downloads, model_installations, model_artifacts, runtime_installations, runtime_instances, schedules (Engineering Program Section 7.3). -->
