# operations-go testdata

Deterministic test inputs for the Go service (e.g., local HTTP download server payloads,
signed runtime test stub references). No PHI; no live secrets.

<!-- TODO: Add fixtures referenced by jobs, downloads, models, and runtime tests. -->
