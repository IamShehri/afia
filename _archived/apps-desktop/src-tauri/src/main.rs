// AFIA desktop binary entry point.
// The Tauri shell composes concrete Rust adapters and starts the application.
// It contains no domain rules and no SQL.
// TODO(S03-T05): Initialize bootstrap, build the composition root, and run Tauri.

fn main() {
    // TODO(S03-T05): bootstrap(); let app = composition::build(); app.run();
}
