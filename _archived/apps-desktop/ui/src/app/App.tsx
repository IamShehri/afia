// AFIA application shell.
// App shell owns providers, startup, router, and error boundaries only — no feature behavior.
// TODO(S03-T04): Compose providers, memory router, and error boundaries.

export function App() {
  // TODO(S03-T04): Render the static shell (no business logic).
  return null;
}
