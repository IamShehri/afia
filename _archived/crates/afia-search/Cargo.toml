# afia-search — indexing, health, query, ranking, rebuild.
# Dependency rule: domain, application ports.
[package]
name = "afia-search"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# TODO: afia-domain, afia-application (ports).
