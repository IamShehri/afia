# afia-plugins

v1 internal plugin contracts and the disabled-by-default host gate.

**Depends on:** domain, contracts, application ports.

<!-- TODO: Modules — manifest, permission vocabulary, descriptors, compatibility validation, built-in registry, disabled host gate, conformance fixtures. Discovery/execution stay disabled in v1; permissions default to denied. -->
