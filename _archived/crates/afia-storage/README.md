# afia-storage

SQLCipher connections, migrations, repositories, and encrypted object storage.

**Depends on:** domain, application ports, security.

<!-- TODO: Modules — global DB, workspace DB, connection policy (S05-T02), migrations (S05-T01/T03), repositories, object staging/promotion/read/GC (S06-T02), transaction unit, integrity check, test DB support, recent_projects (S06-T04). -->
