# afia-ai

Capability registry, policy routing, Python client, and run provenance.

**Depends on:** domain, contracts, application ports, security.

<!-- TODO: Modules — capability registry, model/capability descriptors, policy router, provider adapter interface, model adapter interface, Python HTTP client (S04-T06), segmenter, result validator, span mapper, run recorder, comparison, OpenMed adapter. -->
