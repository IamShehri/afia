//! afia-ai — capability registry, policy routing, Python client, run provenance.
//!
//! TODO: Implement modules (Section 13): capability registry, model/capability
//! descriptors, policy router, provider adapter interface, model adapter interface,
//! Python HTTP client, segmenter, result validator, span mapper, run recorder,
//! comparison, OpenMed adapter.
//!   - python_client   TODO(S04-T06): authenticated loopback HTTP client to Python.
//!
//! The frontend never calls Python and never constructs an OpenMed payload.
//! Skeleton only — no implementation.
