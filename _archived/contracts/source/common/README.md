# contracts/source/common

Common contract metadata and stable error schemas shared by every contract.

<!-- TODO(S04-T01): request.yaml, response.yaml, error.yaml — request ID, contract version, timestamp, error category, retryability, safe details. No provider-specific error. -->
