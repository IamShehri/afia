# contracts/source/ai-runtime

Rust–Python HTTP contracts (Engineering Program Section 8.4). Loopback-only, per-launch
bearer secret, browser-origin rejection, payload limits, no durable request content.

<!-- TODO(S04-T03): health.yaml; later /v1/capabilities and /v1/executions. Python never returns a workspace path. -->
