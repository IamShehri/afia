export const COOKIE_NAME = "app_session_id";
export const ONE_YEAR_MS = 1000 * 60 * 60 * 24 * 365;

/**
 * Fallback base URL when window is unavailable (SSR, scripts, tests).
 * TODO: placeholder — we do not own afia.health; replace when a real domain is purchased.
 */
export const APP_PUBLIC_URL = "http://localhost:3000";
