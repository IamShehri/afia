# packaging/

Release packaging assets and pinned runtime/model definitions.

- `macos/` — macOS Apple Silicon packaging (bundle, signing, notarization, updater).
- `models/` — curated capability-pack candidate definitions and manifests.
- `python/` — managed Python bundle manifest and packaging inputs (no user pip step).

No unpinned runtime dependency.
